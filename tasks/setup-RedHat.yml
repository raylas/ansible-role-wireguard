---
- name: install ELRepo and EPEL repositories
  yum:
    name: '{{ item }}'
    state: present
    update_cache: yes
  with_items:
  - 'elrepo-release'
  - 'epel-release'

- name: install WireGuard packages
  yum:
    name: '{{ item }}'
    state: present
  with_items:
  - 'kmod-wireguard'
  - 'wireguard-tools'
